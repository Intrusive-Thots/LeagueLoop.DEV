---
name: Build Agent UI
description: Auto-generated skill for building dark-themed, glassmorphic agent system prompt configuration web UIs
---

# Build Agent UI

## Context
This skill was auto-generated after successfully building the Kardashev-Infinity-AetherWeaver agent UI.

## Steps
1. **Initialize Directory**: Create a dedicated directory for the UI components.
2. **HTML Structure**: Set up a semantic `index.html` including CSS resets, custom fonts (Outfit, Fira Code), and icon libraries (e.g., Lucide).
3. **CSS Styling**:
    - Use CSS variables for a dark-themed color palette (`#0a0b12` background, purple/cyan accents).
    - Apply glassmorphism via `rgba` backgrounds, blur filters, and subtle borders.
    - Implement a responsive grid layout (typically 350px left column for specs, 1fr right column for prompt).
4. **Agent Profile Representation**:
    - Ensure tags, descriptions, directives, and workflow lists are styled with icons and colored bullets.
    - Highlight the prompt area using a distinctive terminal-like card UI.
